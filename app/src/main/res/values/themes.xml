<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base theme for light mode -->
    <!--
      System bar colors and icon appearance are managed at runtime via
      enableEdgeToEdge() and WindowInsetsControllerCompat. The android:statusBarColor,
      android:navigationBarColor and android:windowLightStatusBar attributes are
      deprecated on Android 15 (API 35) and are intentionally omitted.
    -->
    <style name="Theme.Petit" parent="android:Theme.Material.Light.NoActionBar" />
</resources>
